# 머니냥 개선 일지
**작업일: 2026년 6월 16일**

---

## ✅ 완료된 개선 작업

### 1. PC 반응형 UI 개선 (피드백 10·11·12번 대응)
- PC(769px 이상)에서 헤더 탭바가 세로로 깨지던 문제 수정
- `mobile.css`의 `display:none` 규칙을 PC CSS에서 `!important`로 덮어씌움
- 햄버거(☰) 버튼 PC에서도 표시 → 사이드바 드로어로 슬라이드 열림
- SAO 세로 메뉴 PC에서 완전 숨김
- 달력 칸 높이 균일하게 고정 (130px)
- 태블릿(769~1024px) / 대형(1440px+) 화면 별도 대응
- **수정 파일:** `main.css`

---

### 2. 글자 크기 및 가독성 개선 (피드백 7·9번 대응)
- 전체 JS/HTML 파일 인라인 font-size 약 1.4배 업스케일
  - 10px→14px, 12px→16px, 14px→18px, 16px→21px 등
- PC에서 `body { font-size: 20px }` 시니어 기준 적용
- 입력 필드 최소 높이 50px, font-size 18px 적용
- **수정 파일:** `budget.js`, `calendar-modes.js`, `freelance.js`, `init.js`, `jobtype.js`, `leave.js`, `notifications.js`, `render-salary.js`, `salary.js`, `storage.js`, `ui.js`, `index.html`

---

### 3. 탭 전환 버그 수정 (피드백 6번 대응)
- 근태관리·급여관리·대시보드 등 탭 클릭해도 화면 전환 안 되던 버그 수정
- 원인: CSS에서 `#att-page { display: block }` 강제 설정이 JS의 `showPage()` 제어를 덮어쓰고 있었음
- 해결: CSS에서 페이지 div의 `display` 강제값 전체 제거 → JS가 단독 제어
- **수정 파일:** `main.css`

---

### 4. 연차/결근 사유 입력 팝업 추가 (피드백 5번 대응)
- 기존: 연차/결근 버튼 클릭 → 즉시 저장 (사유 없음)
- 변경: 클릭 → 사유 입력 팝업 → 저장
- 팝업 구성:
  - 빠른 선택 버튼 (개인 사정 / 병원 방문 / 가족 행사 / 여행 / 경조사 등)
  - 직접 입력 텍스트 박스
  - 취소 / 저장 버튼
- 저장 후 달력 셀에 사유 텍스트 직접 표시 (`📝 병원 방문`)
- ESC 키로 팝업 닫기 지원
- **수정 파일:** `index.html`, `ui.js`

---

### 5. 반차/조퇴 사유 입력 팝업 추가 (피드백 5번 대응)
- 기존: 반차/조퇴 클릭 → 바로 시간 설정 UI
- 변경: 클릭 → 사유 입력 팝업 → 저장 → 시간 설정 UI로 자동 이동
- 사유 저장 후 시간 설정까지 완료해야 최종 기록됨
- 빠른 선택 사유 각각 다르게 설정
  - 반차: 병원 방문 / 오전 반차 / 오후 반차 / 가족 행사 등
  - 조퇴: 병원 방문 / 몸이 아파서 / 가족 돌봄 등
- **수정 파일:** `index.html`, `ui.js`

---

### 6. 날짜 팝업 버튼 UI 전면 개편 (피드백 4·8·9번 대응)
- 기존: 작은 태그형 버튼 (font-size 12px) 가로 나열
- 변경: 2열 그리드, 큰 버튼 (font-size 17px), 아이콘+제목+설명 3줄 구조
- 버튼 배치 순서 (아코디언 안):
  - 🌿 연차 / ❌ 결근
  - ⏰ 반차 / 🚶 조퇴
  - 🌙 휴일근무 / 🏛️ 공휴일
  - 🔵 토요특근 / 🔴 일요특근
  - 🗑️ 이날 기록 초기화 (전체 너비)
- **수정 파일:** `index.html`

---

### 7. 기능별 설명 문구 추가 (피드백 4·9번 대응)
- 각 버튼에 한 줄 설명 표시:

| 버튼 | 설명 문구 |
|------|-----------|
| 🌿 연차 | 유급 하루 쉬는 날 |
| ❌ 결근 | 무단·무급 결석 |
| ⏰ 반차 | 오전 또는 오후 반일 |
| 🚶 조퇴 | 정시 전 퇴근·급여 공제 |
| 🌙 휴일근무 | 법정공휴일 출근 ×2.0 |
| 🏛️ 공휴일 | 유급 쉬는 날 (급여 공제 없음) |
| 🔵 토요특근 | ×1.5 수당 적용 |
| 🔴 일요특근 | 일반 일요일 출근 ×2.0 |

- 마우스 오버 시 툴팁으로 상세 설명 추가
- **수정 파일:** `index.html`

---

### 8. 데이터 초기화 2단계 확인 (피드백 3번 대응)
- 기존: 🗑️ 초기화 버튼 클릭 → 즉시 삭제
- 변경: 클릭 → 확인 팝업 (`○월 ○일 기록을 삭제할까요?`) → 확인 시 삭제
- 실수로 데이터 삭제되는 사고 방지
- **수정 파일:** `ui.js`

---

### 9. 공휴일 계산 기준 명확화
- 계산 로직은 원래부터 5인 이상 기준 유급 처리로 구현돼 있었음 확인
- 잘못된 설명 문구(`5인미만 무급`) → `유급 쉬는 날 (급여 공제 없음)`으로 수정
- 휴일근무 vs 공휴일 vs 일요특근 개념 명확히 분리
- **수정 파일:** `index.html`

---

## 🔜 다음 예정 작업

| 우선순위 | 항목 | 피드백 번호 |
|---------|------|-----------|
| 🔥 높음 | 앱 최초 실행 시 온보딩 튜토리얼 | 1번 |
| 🔥 높음 | 사용설명서 진입 버튼 UI 추가 | 2번 |
| 🔥 높음 | 급여관리·대시보드 탭 전환 최종 확인 | 6번 |
| 중간 | 비숙련 사용자 기준 사용성 테스트 | 8·9번 |
| 중간 | 5인 미만 사업장 모드 설정 추가 | - |
| 낮음 | 사용자 피드백 수집 체계 구축 | 13번 |

---

*머니냥 v1.x — 모두의창업 1기 개선 작업*
